<form method="post" action="{{ path('app_insurance_category_delete', {'id': insurance_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ insurance_category.id) }}">
    <button class="btn">Delete</button>
</form>
