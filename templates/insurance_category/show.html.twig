{% extends 'base.html.twig' %}

{% block title %}InsuranceCategory{% endblock %}

{% block body %}
    <h1>InsuranceCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insurance_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ insurance_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ insurance_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_insurance_category_index') }}">back to list</a>

    <a href="{{ path('app_insurance_category_edit', {'id': insurance_category.id}) }}">edit</a>

    {{ include('insurance_category/_delete_form.html.twig') }}
{% endblock %}
