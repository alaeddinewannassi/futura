{% extends 'base.html.twig' %}

{% block title %}InsuranceGuarantee{% endblock %}

{% block body %}
    <h1>InsuranceGuarantee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insurance_guarantee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ insurance_guarantee.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ insurance_guarantee.description }}</td>
            </tr>
            <tr>
                <th>Coverage</th>
                <td>{{ insurance_guarantee.coverage }}</td>
            </tr>
            <tr>
                <th>MonthlyPrice</th>
                <td>{{ insurance_guarantee.monthlyPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_insurance_guarantee_index') }}">back to list</a>

    <a href="{{ path('app_insurance_guarantee_edit', {'id': insurance_guarantee.id}) }}">edit</a>

    {{ include('insurance_guarantee/_delete_form.html.twig') }}
{% endblock %}
